"""place shared models here, to import, just use : 
from jdd_community_backend.models import SoftDelete
"""

"""
from django.db import models

class SoftDelete(models.Model):
    deleted_at = models.DateTimeField(default=None, null=True)
    
    class Meta:
        abstract = True
"""